version: '3.8'

services:

  backend:
    build:
      context: ./backend
    container_name: weather_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/weather.db:/app/weather.db
    restart: unless-stopped
